services:
  db:
    image: postgres:16
    restart: always
    environment:
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=changelethis123
      - POSTGRES_DB=app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
